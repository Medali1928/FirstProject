<head>
    <meta charset="UTF-8">
    <title>Liste des Auteurs</title>
    <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
</head>
<body>
<h1>Détails de l'auteur</h1>

<p><strong>Nom :</strong> {{ author.username }}</p>
<p><strong>Email :</strong> {{ author.email }}</p>
<p><strong>Nombre de livres :</strong> {{ author.nb_books }}</p>
<p><img src="{{ asset(author.picture) }}" alt="{{ author.username }}" width="150" height="150"></p>

<a href="{{ path('list_author') }}">Retour à la liste</a>
</body>